# 引継書 — パチスロ収支表 PWA

**目的**：本アプリの資産を、家計簿的な収支管理アプリへ流用するための引継資料。
**対象読者**：本リポジトリを初見で触る開発者（新しいセッションのAIエージェントを含む）。
**最終更新**：2026-08-04 / 対応コミット `e6b9f09`（Service Worker キャッシュ v25）

---

## 1. このアプリの正体

スマホ（iPhone）でホーム画面に追加して使う、**完全ローカル動作のPWA**。パチスロの1日ごとの「投資額／回収額」を記録し、収支・勝率・月別集計・年間グラフを表示する。

- サーバー・API・ログインは**一切ない**。データは端末のIndexedDBのみ。
- ビルドツールなし。HTML/CSS/JSの素のファイルをそのまま配信するだけ。
- 外部依存は Google Fonts（Outfit / DM Mono）のみ。npm パッケージはゼロ。

**公開URL**：https://yuyaman-lab.github.io/ledger/

---

## 2. 技術スタック

| 項目 | 採用技術 | 備考 |
|---|---|---|
| 言語 | HTML / CSS / バニラJS（ES2020） | フレームワーク・ビルド一切なし |
| データ | IndexedDB | DB名 `slot_ledger_db` / ストア `entries` |
| 設定保存 | localStorage | ロック設定・PINハッシュ等 |
| PWA | Service Worker + manifest | オフライン動作 |
| 認証 | WebAuthn（パスキー）+ PIN | 端末内で完結、サーバー検証なし |
| グラフ | Canvas 2D 手描き | ライブラリ不使用 |
| ホスティング | GitHub Pages | `main` push で自動デプロイ |

**フレームワークを入れなかった理由**：画面数が少なく、起動速度（特にPWAの初回描画）を最優先したため。家計簿アプリでも画面数が10を超えないなら、この構成のまま増築するのが速い。

---

## 3. ファイル構成

```
ledger/
├── index.html              505行  全画面のマークアップ + スプラッシュ用インラインCSS
├── app.js                 1337行  全ロジック（単一ファイル）
├── styles.css              624行  全スタイル
├── sw.js                    62行  Service Worker
├── manifest.webmanifest.txt        PWAマニフェスト ※後述の既知問題あり
└── .github/workflows/       GitHub Pages 自動デプロイ
```

### app.js のセクション構成（行番号は現時点）

| 行 | セクション | 流用可否 |
|---|---|---|
| 1–578 | スプラッシュ画面（スラムイン → ネオン点灯 → チャージ → 静寂 → 爆発）<br>タイムラインは冒頭の `at(ms, fn)` 呼び出しに集約。演出の尺はここだけで調整できる。<br>背景の集中線・衝撃波・火花は末尾の `createBackdrop()` が担当（`#splashBg` キャンバス）。<br>`prefers-reduced-motion: reduce` の端末では演出を省いて静止表示する。 | 演出のみ。文言差し替えで流用可 |
| 579–591 | ユーティリティ（`$`, `fmtYen`, `ym`, `uid` 等） | **そのまま流用** |
| 592–623 | IndexedDB ラッパ（open/getAll/put/delete/clear） | **そのまま流用**（DB名のみ変更） |
| 624–673 | ロック（PIN + パスキー） | **そのまま流用** |
| 674–690 | 状態管理・フィルタ | ほぼ流用（`profitOf` は要改修） |
| 691–765 | タブ・フィルタUI・一覧描画 | 構造は流用、項目は要改修 |
| 766–795 | 入力モーダル | 構造は流用、入力項目は要改修 |
| 796–806 | `switchToTab` | **そのまま流用** |
| 807–956 | サマリー（ヒーローカード）+ `fitHeroAmount` | **そのまま流用**（後述） |
| 957–1030 | 月別カルーセル | 構造は流用、表示項目は要改修 |
| 1031–1183 | 年間グラフ（Canvas） | 流用可だが**Y軸が固定値**。要改修 |
| 1184–1235 | CSV出力 / JSONバックアップ・復元 | 列定義のみ変更して流用 |
| 1236–1256 | ロックUI | **そのまま流用** |
| 1257–1337 | 初期化（`main()`）・イベント束ね（`bindUI()`） | **そのまま流用** |

---

## 4. データモデル

### 現状（パチスロ）

```js
{
  id:         "uuid",        // crypto.randomUUID()
  date:       "2026-06-28",  // ISO日付（YYYY-MM-DD）
  investment: 25000,         // 投資額（円）
  payout:     41000,         // 回収額（円）
  memo:       "アイム@56"     // 任意
}
```

収支は保存せず、**都度計算**する：`profitOf(e) = payout - investment`（app.js:262）

### 家計簿への拡張案

最小の変更で済ませるなら、`investment`→`支出`、`payout`→`収入` と読み替えるだけでも動く。ただし家計簿は**カテゴリ別集計**が要になるので、以下の追加を推奨：

```js
{
  id:       "uuid",
  date:     "2026-06-28",
  type:     "expense",       // "income" | "expense"  ★追加
  amount:   3200,            // 金額（正の数で保持）  ★investment/payoutを統合
  category: "食費",           // カテゴリ              ★追加
  account:  "現金",           // 口座・支払手段（任意） ★追加
  memo:     "スーパー"
}
```

**移行時の注意**：IndexedDBのスキーマ変更は `openDB()` の `indexedDB.open(DB_NAME, 1)` のバージョン番号を上げ、`onupgradeneeded` で対応する（app.js:179–189）。既存データを引き継がないなら、**DB名ごと変える**のが安全かつ簡単（例：`kakeibo_db`）。旧アプリのデータと衝突しない。

---

## 5. 描画アーキテクチャ

サーバーがないので、状態は `entries` 配列1本。更新は必ずこの流れ：

```
DB更新 → loadAndRender()
           ├─ getAll()        … entries を全件読み直す
           ├─ renderFilters() … 年/月セレクトを再構築
           ├─ renderLedger()  … 一覧を innerHTML で再構築
           └─ renderSummary() … 集計 → カルーセル → 年間グラフ → fitHeroAmount
```

**差分更新は一切していない。** 全件読み直して全描画。数千件までは体感で問題なし。家計簿は件数が増えやすいので、**1万件を超えるようなら**ここが最初のボトルネックになる（対策：月フィルタをDB側のインデックスで絞る。`store.createIndex("date","date")` は既に張ってある）。

---

## 6. UI設計の重要な決定事項（ここが一番の資産）

家計簿アプリでも同じ問題に必ずぶつかるので、**結論だけ先に読むこと**。

### 6.1 「ファーストビューに全カードが収まる」が最優先要件

スクロールなしでホーム画面の全情報が見えることを絶対条件として設計している。実現方法：

- `body` を **flex縦積み**にし、`.container` を `flex:1; min-height:0`（styles.css）
- 余ったスペースは**年間グラフが吸収**する（`.year-graph{flex:1 1 auto}`）
- カード間に `justify-content:space-between` を**使わない**（不自然な隙間が空くため）

### 6.2 ヘッダー高さのマジックナンバーを排除済み

以前は `height:calc(100dvh - 52px - 84px)` とヘッダー52px前提で固定していたが、**ヘッダーの文字を拡大した瞬間に破綻**した（65pxになり下部がナビに隠れた）。現在はflexで自動追従する。**この方式を必ず引き継ぐこと。**

```css
body{ display:flex; flex-direction:column; padding-bottom:84px; } /* 84px = 固定ナビの高さ */
.container{ flex:1; min-height:0; }
```

### 6.3 金額のフォント自動縮小（`fitHeroAmount`, app.js:511）

総収支額の桁が増えても、右横のバッジと重ならないようフォントを自動縮小する。家計簿は金額の桁がブレやすいので**そのまま流用推奨**。

仕組み：一度フォント指定を空にしてCSS基準値を取得 → 実測幅がはみ出す分だけ縮小（下限30px）。`renderSummary` の末尾と `resize` イベントで呼ぶ。

### 6.4 CSSメディアクエリの罠（ハマりどころ）

高さ別に `@media (max-height:880px)` / `(max-height:740px)` で詰めているが、**メディアクエリを先に書き、基本ルールを後に書くと、詳細度が同じなので基本ルールが勝ってしまう**。これで一度ハマった。

対策として、メディアクエリ内は `#tab-summary` を前置して詳細度を上げてある：

```css
@media (max-height:880px){
  #tab-summary .hero-card{ padding:14px 24px }   /* ← #tab-summary が必須 */
}
```

### 6.5 折り返し対策

日本語UIは**すぐ2行に折り返して高さが崩れる**。以下は `white-space:nowrap` を明示している：金額（`.hero-amount` `.mc-profit` `.profit`）、バッジ、メタラベル（`.meta-k`）、ボタン（`.btn`）。横並びのFlexアイテムには `min-width:0`（左）／`flex-shrink:0`（右）をセットで指定する。

---

## 7. デプロイと Service Worker（**最重要・事故が起きやすい**）

### 7.1 デプロイ

`main` に push するだけで GitHub Actions が GitHub Pages へ自動公開する。ビルド工程なし（`path: '.'` を丸ごとアップロード）。

### 7.2 Service Worker のキャッシュ戦略

**過去に最も時間を溶かした問題がこれ。** 当初 cache-first だったため、修正をデプロイしても「**アプリを2回起動しないと反映されない**」状態になり、レイアウトが直っていないと誤認して何度も無駄な修正を重ねた。

現在は **network-first**（sw.js:35–62）に変更済み：毎回ネットワークから取りに行き、成功したらキャッシュを更新、失敗（オフライン）時のみキャッシュを返す。

> **必ず守るルール**：`index.html` / `styles.css` / `app.js` のいずれかを変更したら、**`sw.js` の `CACHE_NAME` を必ずインクリメントする**（現在 `slot-ledger-v25`）。忘れると古いキャッシュが残る。

### 7.3 manifest の 404（修正済み・v26）

以前は `index.html:7` が `manifest.webmanifest` を参照しているのに実ファイルが **`manifest.webmanifest.txt`**（拡張子が `.txt` のまま）で、**マニフェストが常に404**だった。PWAのインストール名・テーマカラーが効かない状態。

- **対処済み**：正しい名前へリネームし、Service Worker の `CORE_ASSETS` にも追加（オフライン時も配信される）。実測で `GET /manifest.webmanifest => 200` を確認。
- 新アプリでは最初から正しい拡張子で置くこと。**`.txt` が付いていると静的ホスティングでは黙って404になり、コンソールを見るまで気づけない。**
- なお `icons: []` は空のままなのでホーム画面アイコンは未設定。家計簿アプリではアイコン画像を用意すること。

---

## 8. 家計簿アプリへの改修ポイント（作業見積り付き）

| # | 対象 | 内容 | 規模 |
|---|---|---|---|
| 1 | `DB_NAME` | `kakeibo_db` 等へ変更（旧データと分離） | 小 |
| 2 | データモデル | `type` / `category` / `account` を追加、`amount` に統合 | 中 |
| 3 | `profitOf()` | 収入−支出の合計計算へ書き換え | 小 |
| 4 | 入力モーダル | 収入/支出トグル + カテゴリ選択を追加 | 中 |
| 5 | 一覧 | カテゴリ表示、色分け | 小 |
| 6 | サマリー | 「勝率／回収率」→「収支バランス／貯蓄率」等へ | 中 |
| 7 | **年間グラフ** | **Y軸が固定値**（`Y_MIN=-200000, Y_MAX=1050000`, app.js:650）。家計簿は金額レンジが違うので**データから動的算出に変更必須** | 中 |
| 8 | カルーセル | 月別カード。カテゴリ別内訳の表示に拡張すると価値が高い | 中 |
| 9 | CSV出力 | 列定義を変更（app.js:766） | 小 |
| 10 | 文言・スプラッシュ | 「パチスロ収支表」→ 新名称 | 小 |
| 11 | manifest | 名称・テーマカラー変更 + アイコン設定（拡張子は修正済み） | 小 |

**新規に欲しくなる機能**（本アプリにない）：カテゴリ別集計・円グラフ、予算設定と超過アラート、固定費の繰り返し登録、月またぎの繰越表示。

---

## 9. 開発・検証の進め方（実践手順）

### 9.1 ローカル起動

```bash
python -m http.server 8000
```

`file://` で開くと Service Worker と IndexedDB が動かないため、**必ずHTTPサーバー経由**で開く。

### 9.2 実機サイズでの検証

レイアウトは必ず**実機サイズ**で確認する（PCの縦長ウィンドウでは問題が出ない）。検証に使ったサイズ：

- iPhone 14/15：**393 × 852**（基準）
- iPhone Pro Max：430 × 932
- iPhone SE：375 × 667（最小。ここで崩れやすい）

Playwrightで `browser_resize` → `browser_navigate` の順で開く。**ナビゲートすると viewport がリセットされることがある**ので、計測前に `window.innerHeight` を必ず確認すること。

### 9.3 テストデータの投入

IndexedDBに直接流し込むのが速い。**フィールド名を間違えると一覧が空になる**（`investment` を `invest` と書いて一度ハマった）ので注意。

```js
const db = await new Promise(r=>{const q=indexedDB.open('slot_ledger_db');q.onsuccess=()=>r(q.result)});
const os = db.transaction('entries','readwrite').objectStore('entries');
os.clear();
os.put({id:'1', date:'2026-06-28', investment:2000, payout:41000, memo:'テスト'});
```

> **注意**：テストデータの投入は必ず `localhost` に対して行う。本番（github.io）は別オリジンなので実データは影響を受けないが、取り違えないこと。

### 9.4 レイアウト崩れの判定基準

見た目ではなく**数値で判定**する。以下が確実：

```js
const ps = document.querySelector('#tab-summary .panel-scroll');
ps.scrollHeight - ps.clientHeight;   // 0 なら全カードが収まっている
```

### 9.5 ローカルでSWが邪魔をするとき

```js
navigator.serviceWorker.getRegistrations().then(rs=>rs.forEach(r=>r.unregister()));
caches.keys().then(ks=>ks.forEach(k=>caches.delete(k)));
```

---

## 10. 引き継ぎ時の注意（過去の失敗から）

1. **「反映されない」と感じたら、まずキャッシュを疑う。** コードを触る前に、実際に配信されているファイルの中身を確認すること。過去、正常なコードを「壊れている」と誤認して修正を重ね、大幅に手戻りした。
2. **スプラッシュ画面のインラインCSS（index.html内）にある `*{margin:0;padding:0}` と `html,body{...}` を消さないこと。** 「重複だから」と削除したところ、全体のフォントサイズとレイアウトが崩れた。これは元から必要なリセットである。
3. **同じファイルを二重に読み込まない。** `app.js` を2回 `<script>` した結果、`Identifier already declared` でスプラッシュが真っ白になった事故がある。
4. **文字を大きくする要求は、必ず「収まるか」とセットで検証する。** 拡大→はみ出し→他要素を縮める、のループになりやすい。数値（9.4）で確認してから完了報告すること。

---

## 11. 現状の未対応事項

- [x] ~~`manifest.webmanifest.txt` のリネーム（404）~~ → v26 で修正済み
- [ ] PWAアイコン未設定（`icons: []`）
- [ ] 年間グラフのY軸が固定値（データに応じた自動スケールなし）
- [ ] `.playwright-mcp/` や検証用スクリーンショット（`verify-*.png`）が未整理

---

## 12. セキュリティ設計（家計簿アプリでも必ず引き継ぐこと）

サーバーを持たない設計のため、**そもそも漏洩する経路がない**のが最大の強み。リポジトリを公開しても、収支データは端末のIndexedDBにしか存在しない。この前提は崩さないこと。

### 12.1 守るべき3つのルール

1. **画面に出す値は必ず `escapeHtml()` を通す**
   `innerHTML` を使う箇所（一覧・カルーセル・フィルタ）で、1つでも素通りさせると保存型XSSになる。過去に `date` だけエスケープ漏れがあり、悪意あるバックアップJSONの取り込みで任意コード実行が可能な状態だった（修正済み）。
   
2. **復元（インポート）は信頼できない入力として扱う**
   外部ファイル由来のデータは、型・形式をすべて検証してから保存する。`restoreJSON()` の実装がひな形になる。日付は `isValidISODate()` で実在日まで確認している（`2026-13-99` のような形式だけ合う値を弾くため）。

3. **CSPを維持する**
   `index.html` の `<meta http-equiv="Content-Security-Policy">` で `script-src 'self'` を指定済み。これにより、万一エスケープを漏らしても `onerror=` 等のインラインハンドラは実行されない（二重防御）。
   **インライン `<script>` を書くとCSPで動かなくなる**ので、JSは必ず外部ファイルにすること（`boot.js` はこのために外部化した）。

### 12.2 ロック機能の正体（誤解しないこと）

PIN・パスキーによるロックは **UI上の目隠しであり、データ保護ではない**。

- データは平文でIndexedDBに保存されている
- 解除判定はlocalStorageのフラグ1つ（書き換えれば解除できる）
- `authPasskey()` は署名検証をしていない（サーバーがないため原理的に不可能）

**端末を他人に渡す前提なら、この仕組みでは守れない。** 本当に保護するなら、PINから `PBKDF2` で鍵を導出して `WebCrypto` でデータ本体を暗号化する必要がある。家計簿は収支表より機微度が高いので、要件次第では最初から暗号化を検討すること。

### 12.3 コミットしてはいけないもの

`.gitignore` で以下を除外済み。**バックアップJSON/CSVは実データそのもの**なので、絶対にコミットしないこと。

```
slot_ledger_backup*.json / *.csv / verify-*.png / .playwright-mcp/
```

---

## 13. 参照

- 公開URL：https://yuyaman-lab.github.io/ledger/
- リポジトリ：https://github.com/Yuyaman-lab/ledger
- 直近の主要コミット
  - `e6b9f09` 一覧詳細・ヘッダー・フッター拡大、flexレイアウト化
  - `5c4513c` バッジ移動、総収支額のフォント自動縮小
  - `18a4499` モーダル・一覧の文字拡大
